\* Apalache BMC (bounded, finite constants):
\*   apalache-mc check --length=k --config=APCasInd.cfg APCasInd.tla
\* State inv `StrengthenedCasFresh' + action inv `StrengthenedCasFreshInductiveStep'
\* (I /\ Next => I') are checked on symbolic runs; see module header for limits.
CONSTANTS
  K = 4
  L = 2
  fps = {1, 2, 3, 4}
  empty = 0
  Writer = { "w1_OF_Writer", "w2_OF_Writer" }
  Reader = { "r_OF_Reader" }

SPECIFICATION ApalacheSpec

INVARIANT
  StrengthenedCasFresh
  StrengthenedCasFreshInductiveStep

CHECK_DEADLOCK FALSE
