\* Inductive check (section 1.5): use commands in APCasInd.tla header with
\* --inv=StrengthenedCasFresh (--length=0 from Init; --length=1 from I).
\* Bounded BMC: apalache-mc check --length=k --config=APCasInd.cfg APCasInd.tla
\* also lists the action invariant `StrengthenedCasFreshInductiveStep' below.
CONSTANTS
  K = 4
  L = 2
  fps = {1, 2, 3, 4}
  empty = 0
  Writer = { "w1_OF_Writer", "w2_OF_Writer" }
  Reader = { "r_OF_Reader" }

SPECIFICATION ApalacheSpec

INVARIANT
  StrengthenedCasFresh
  StrengthenedCasFreshInductiveStep

CHECK_DEADLOCK FALSE
