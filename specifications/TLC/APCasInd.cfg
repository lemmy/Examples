\* Apalache: apalache-mc check --length=8 --config=APCasInd.cfg APCasInd.tla
\* (`--length' bounds BMC depth; StrengthenedCasFresh held through depth 8.)
CONSTANTS
  K = 4
  L = 2
  fps = {1, 2, 3, 4}
  empty = 0
  Writer = { "w1_OF_Writer", "w2_OF_Writer" }
  Reader = { "r_OF_Reader" }

SPECIFICATION ApalacheSpec

INVARIANT
  StrengthenedCasFresh

CHECK_DEADLOCK FALSE
